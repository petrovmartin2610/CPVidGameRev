@model IEnumerable<CPVidGameRev.Models.GameGenre>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Genre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TimeAdded)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AverageUserRating)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MinUserRating)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MaxUserRating)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.Genre)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.TimeAdded)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.AverageUserRating)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.MinUserRating)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.MaxUserRating)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
        @Html.ActionLink("Details", "Details", new { id = item.Id }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.Id })
    </td>
</tr>
}

</table>
